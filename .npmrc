
# Enforce npm as package manager
package-manager=npm

# Disable package-lock generation during install (we manage it manually)
package-lock=true

# Use npm registry
registry=https://registry.npmjs.org/

# Disable audit warnings for faster installs
audit=false

# Use exact versions for better reproducibility
save-exact=true

# Disable funding messages
fund=false
